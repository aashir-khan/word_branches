import 'package:flutter/material.dart';

const Color primaryColor = Color.fromRGBO(191, 10, 48, 1);
const Color primaryColorLight = Color.fromRGBO(248, 80, 89, 1);
const Color primaryColorDark = Color.fromRGBO(135, 0, 10, 1);

const Color secondaryColor = Color.fromRGBO(15, 156, 230, 1);
const Color secondaryColorLight = Color.fromRGBO(102, 205, 255, 1);
const Color secondaryColorDark = Color.fromRGBO(0, 110, 179, 1);

const Color surface = Colors.white;
const Color background = Colors.white;
const Color error = Color.fromRGBO(176, 0, 32, 1);

const Color onPrimary = Colors.white;
const Color onSecondary = Colors.black;
const Color onSurface = Colors.black;
const Color onBackground = Colors.black;
const Color onError = Colors.black;

const Brightness brightness = Brightness.light;

const Color inputTextHint = Color.fromRGBO(207, 216, 220, 1);
